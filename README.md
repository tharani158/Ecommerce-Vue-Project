# Vue 3 E-Commerce (DummyJSON)

This is a small Vue 3 + TypeScript (strict) SPA that consumes DummyJSON products. It includes Pinia-based cart persistence, auth simulation, Tailwind + dark mode, and routing.

Quick start:

```bash
npm install
npm run dev
```
